# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor folders
.vscode/
.idea/

# Temp exports (don't accidentally commit dept data to the repo)
*_export.json

# Local project memory — not for remote
CLAUDE.md

# Auth token — never commit credentials
.mcp_token.json

# Auto-refresh log — local machine only
dc499_autorefresh.log

# Runtime state — must not be stashed/reverted by git push agent
.sent_batches.json
active_tasks.json

# Local-only files — personal/sensitive, not for public repo
Dean_Karim_Resume.pdf
DC499_Handoff_Guide.pdf
Putwall_map.html
